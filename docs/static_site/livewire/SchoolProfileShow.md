# SchoolProfile / Show

**Class:** `App\Livewire\Cms\SchoolProfile\Show`  
**View:** `livewire.cms.school-profile.show`  
**Layout title:** `School Profile`

## Purpose

Shell/container component that renders the school profile CMS page. All logic lives in `SchoolProfile\Form` — `Show` just provides the layout wrapper and renders the view.

## Note

No state, no methods beyond `render()`. If you need to add read-only display logic alongside the form (e.g. a preview panel), add it here.
